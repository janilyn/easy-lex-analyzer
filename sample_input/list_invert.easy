procedure invert(l)
   node(info,link)
   beta = lambda
   alpha = l
   while alpha != lambda do
      gamma = beta
      beta = alpha
      alpha = link(beta)
      link(beta) = gamma
   endwhile
   l = beta
end invert